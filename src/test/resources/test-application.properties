# Test Configuration
# This file is used for unit testing, integration tests use dynamically generated config

# Kafka Settings
kafka.bootstrap.servers=localhost:9092
kafka.topic.source=test-source-topic
kafka.topic.sink=test-sink-topic
kafka.streams.application.id=test-kafka-cassandra-processor
kafka.streams.exactly.once=true
kafka.streams.commit.interval.ms=100
kafka.streams.num.stream.threads=1
kafka.streams.state.dir=/tmp/kafka-streams-test

# Cassandra Settings
cassandra.contact-points=localhost
cassandra.port=9042
cassandra.keyspace=test_keyspace
cassandra.table=test_data_table
cassandra.local-datacenter=datacenter1
cassandra.username=
cassandra.password=

# Health Check Settings
health.check.port=8081 